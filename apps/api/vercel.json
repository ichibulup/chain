{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          ".express/**",
          "prisma/**",
          ".env.local"
        ],
        "buildCommand": "pnpm build"
      }
    }
  ],

  "routes": [
    {
      "src": "/(.*)", "dest": "/api/index.js"
    }
  ]
}
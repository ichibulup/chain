import { prisma } from '@/lib/prisma';

export const RetailProduct = prisma.retailProduct
export const Cart = prisma.cart
export const CartItem = prisma.cartItem
export const CartItemOption = prisma.cartItemOption
